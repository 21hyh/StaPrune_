 在/mnt/sdc/hqz/yaohui/StaPrune_main/Qwen2_5_VL内，似乎当前的剪枝策略有问题，并不是/mnt/
  sdc/hqz/yaohui/StaPrune_main/LLaVA的策略。我需要改成/mnt/sdc/hqz/yaohui/StaPrune_main/
  LLaVA的策略。但是由于llava和Qwen有架构上的区别，所以难免有些以适配性为目的的兼容性区
  别。

  我先对比 LLaVA 和 Qwen2_5_VL 里当前剪枝实现的入口、路由和 token 选择逻辑，确认现在到底
  偏离了哪一层策略，再直接改成同一套流程。

• 我已经定位到 LLaVA 的模型主干文件；Qwen2_5_VL 这边文件名没有直接带这些关键词，下一步改
  成查代码内容里的 tok_keep_ratio、prune_method 和 selection_strategy，把真正生效的剪枝路
  径找出来。

• 入口已经找到了。Qwen2_5_VL 其实有两套实现：一个独立的 qwen2_5_vl_pruning.py，一个直接嵌
  进 qwen2_5_vl_StaPrune.py 的版本；我现在直接对照 LLaVA/clip_encoder.py 的三个策略函数和
  Qwen 里的同名逻辑，找出偏差点。

• 偏差点已经显出来了：Qwen 这版在选完 token 之后又做了两次插值，把长度“补回去”，这和
  LLaVA 的真实剪枝不一样，等于只改了特征没改 token 数。接下来我确认 Qwen 侧视觉输出是怎样
  接进 LLM 的，保证改成“真正减少视觉 token”时只做必要的架构适配。

• Qwen 跟 LLaVA 的关键结构差异已经确认了：LLaVA 的 prompt 里不是“一图一 patch token 占
  位”，所以视觉 token 数可以自由变；Qwen 则要求 image_embeds.shape[0] == input_ids 里的图
  像占位 token 数。我要继续追 processor 和 lmms_eval 这层，看看现在有没有同步缩短占位
  token；如果没有，就得在 Qwen 适配层一起改，而不是只改 vision encoder。

• 我需要把“减少视觉 token”这件事从 vision encoder 一直串到 processor -> input_ids ->
  rope，所以先用一个最小样例把 Qwen 处理器产出的 image_grid_thw 和 <image_pad> 占位数量对
  应关系跑出来，再决定最小改法。

• 处理器脚本已经跑起来了。我在等它把 image_pad 连续段和 grid_thw 的映射打印出来，这会直接
  决定要不要同步改 input_ids 的占位段长度。

• 我已经确认真正的致命偏差了，不只是“插值补回长度”这一层：Qwen 当前分支在剪枝前拿错了
  merge 分组，实际上只在错误的 1/4 token 子集上做选择。接下来我会直接改成更接近 LLaVA 的
  流程：先得到 Qwen 真正送进 LLM 的视觉 token，再在这层做 V139-G / V105 选择；同时把
  input_ids 里的 <|image_pad|> 占位和 rope/grid 一起同步缩短。

• 当前修复计划是：

  1. 改 Qwen2_5_VL/qwen2_5_vl_StaPrune.py
      - 把剪枝位置改到 Qwen 真正输出给 LLM 的视觉 token 层
      - 去掉“选完再插值补回原长度”的假剪枝
      - 用原始 grid_thw 正确切分每张图，避免跨图/错组选择
  2. 改 lmms-eval/lmms_eval/models/qwen2_5_vl.py
      - 在 processor 之后同步缩短 <|image_pad|> 占位段
      - 额外保留一份原始 image_grid_thw 给 vision encoder，用剪枝后的 image_grid_thw 给
  rope / input 对齐
  3. 跑最小验证
      - 先做静态编译检查
      - 再用 processor 最小样例验证占位 token 数和剪枝后 token 数一致

• 我现在开始改代码，先动 qwen2_5_vl_StaPrune.py 的核心剪枝路径。这里会把“错误分组 + 插值补
  长”的逻辑直接删掉，改成按每张图的 merged token 真正做选择。

• 核心 vision 路径已经改完了，现在它会在 Qwen 真正送给 LLM 的视觉 token 上做选择，不再做
  插值补长。下一步是把模型输入侧也同步到新的 token 数，否则 input_ids 里的 <|image_pad|>
  还会和视觉输出对不上。

• 我要改 lmms_eval/models/qwen2_5_vl.py 了。这里会把 processor 生成的 <|image_pad|> 连续
  占位段按目标保留数缩短，并把“原始视觉网格”和“剪枝后 rope 网格”分开传给模型。

• 代码改动已经落完了，我先做静态检查。这里主要看两个文件有没有语法错误，以及新增的参数透
  传链路有没有拼写问题。

• 静态检查通过了。现在我在跑一个不加载整套大模型的最小验证，只检查 processor 产出的 256
  个 <|image_pad|> 是否会被正确缩到目标数量，并且 vision_image_grid_thw / image_grid_thw
  两份网格是否分离成功。

• 最小输入验证是对的：256 -> 30，而且两份 grid 已经分离出来了。现在我补一个小修正，避免矩
  形网格分区时把 0 维 tensor 直接丢给 Python 的 min()。

• 已经把 Qwen 侧的活跃剪枝路径改成接近 LLaVA 的策略了，核心改动在 yaohui/StaPrune_main/
  Qwen2_5_VL/qwen2_5_vl_StaPrune.py:549 和 yaohui/StaPrune_main/lmms-eval/lmms_eval/models/
  qwen2_5_vl.py:224。

  这次修的点有三个。第一，Qwen2_5_VL 之前不是在真正送进 LLM 的视觉 token 上剪，而是在
  merge 前拿错了分组，随后又把长度插值补回去，所以本质上不是 LLaVA 那套“真剪枝”；现在改成
  先得到 Qwen 真正的 merged image tokens，再按 V139-G / V105 做选择，去掉了插值补长，见
  yaohui/StaPrune_main/Qwen2_5_VL/qwen2_5_vl_StaPrune.py:667 和 yaohui/StaPrune_main/
  Qwen2_5_VL/qwen2_5_vl_StaPrune.py:795。第二，Qwen 和 LLaVA 的架构差异决定了只改 vision
  encoder 不够，因为 Qwen 的 input_ids 里有展开后的 <|image_pad|> 占位；所以我在 lmms-
  eval 侧同步把这些占位段缩短到和剪枝后 token 数一致，并单独保留一份原始
  vision_image_grid_thw 给视觉编码器，见 yaohui/StaPrune_main/lmms-eval/lmms_eval/models/
  qwen2_5_vl.py:236 和 yaohui/StaPrune_main/lmms-eval/lmms_eval/models/qwen2_5_vl.py:433。
  第三，针对 Qwen 的动态分辨率，我把 V139-G 的 coverage refinement 从固定正方形网格改成了
  按 grid_h/grid_w 处理矩形网格，这是必要的适配差异，见 yaohui/StaPrune_main/Qwen2_5_VL/
  qwen2_5_vl_StaPrune.py:591。

  我做了两类验证。py_compile 已通过这两个改动文件。另做了一个 processor 级最小验证：Qwen
  原始单图会生成 256 个 <|image_pad|>，在 tok_keep_ratio=0.111, selection_strategy=auto
  下现在会被正确缩到 30 个，同时 image_grid_thw 变成 [[1, 2, 60]]，而
  vision_image_grid_thw 仍保留原始 [[1, 32, 32]]。这和当前 V105 在极低保留率下会多出一个
  merged token 的策略是一致的。

  我还没有替你重跑完整 pope 评测，所以最终精度结果还需要你用原命令再测一遍。建议先直接复
  现你那条 Qwen2.5-VL-7B-Instruct, tok_keep_ratio=0.111 的命令看 pope_accuracy 是否回到接
  近 LLaVA 预期的水平。


› 怎么回事，11.1%压缩率下正常运行，但是33.3%下报错如下。
• 报错原因已经定位了，不是新的架构问题，而是 V139-G 在高保留率下本来就可能产出少于 ceil(N
  * ratio) 的 token。33.3% 进入 V139-G 后，第二阶段最多只能扩到 k_stage1 + 26 个附近，所

• 我现在补验证，重点看 0.333 时占位数会不会从之前的 162 改成和实际 157 对齐。

• 原因找到了。11.1% 能跑通，是因为它走的是 V105，输出 token 数比较好预测；33.3% 走的是
  V139-G，而 V139-G 第二阶段并不一定能把 k_stage2 补满，所以实际输出 token 数会小于
  ceil(N * ratio)。

  你日志里的两个典型例子正好说明了这个问题：
  features and image tokens do not match。

  我已经把这个问题修在 yaohui/StaPrune_main/lmms-eval/lmms_eval/models/qwen2_5_vl.py:229
  了。现在 Qwen 这边缩 <|image_pad|> 不再按 ceil(N * ratio) 死算，而是按当前 V139-G 实现
  - 484 -> 157
  - 531 -> 169
  - 单图 256 tokens 且 ratio=0.333 时，缩到 86

  你现在直接重跑原来的 0.333 命令就行，不需要改参数。我这边没有重跑完整 pope，只做了
  py_compile 和 processor/token-count 对齐验证；如果你重跑后还有新报错，把新 trace 发我。


Qwen 视觉塔配置里正好有 4 个 full-attention blocks ([7, 15, 23, 31])，而
  LLaVA 策略本来就依赖“最后 4 层加权融合”。现在 Qwen 代码只用了最后一个 full-attention block。因为7/15/23/31 全部拿来做融合，并不等价于 LLaVA 的“最后 4 个深层 attention layers”。在 Qwen 里，7 和 15
  明显更浅，直接混进去会把分数分布拉偏。

  Qwen 现在改成在 merger 之前做选择，但真正送进 LLM 的
  token 数是在 merger 之后减少。这比之前“先 merger 再选”更接近 LLaVA 的 prune-before-project 逻辑。关键改动
  Qwen2_5_VL/qwen2_5_vl_StaPrune.py:1015, yaohui/StaPrune_main/Qwen2_5_VL/qwen2_5_vl_StaPrune.py:1031, yaohui/
  StaPrune_main/Qwen2_5_VL/qwen2_5_vl_StaPrune.py:1035。

  具体来说，现在是：

  - 仍然只用最后一个 full-attention block 做 attention score。
  - 先把每个 LLM token 对应的 4 个 patch 作为一个 group。
  - 用 group 的均值特征做 V105 / V139-G 选择。
  - 真正保留下来的是原始 4 patch group，再送进 merger，不是先 merger 后再裁。
  - V105 在极低保留率下那个额外 merged token，也是在 group 级别先合成，再过 merger。

  我做了两个验证。py_compile 已通过。另做了轻量单元验证，当前这条新路径输出 token 数和 lmms-eval 那边的占位

  我还没有替你重跑完整 pope。现在最该做的是你直接重跑 0.111 和 0.333 两组结果看是否回升。如果这版还是偏差明
  显，那么下一个最值得怀疑的点就不是 attention fusion 了，而是“Qwen 现在仍用 final vision-block states 做选
  择特征”，而 LLaVA 用的是显式 select_layer hidden state。

  LaVA 的真实路径是“select_layer 的 hidden state 做 feature，attention 只是做
  score”，而当前 Qwen 仍然是“最后一层输出做 feature”。Qwen 的 merger 不是简单平均，而是 RMSNorm + flatten(4 tokens) + MLP。这意味着我现在用 group mean 做多样性
  选择，本质上已经偏离了原始表征空间。接下来我看视觉主干怎么拿 hidden state，优先把选择特征改成更接近 merger
  输入的真实 group 表征。

  严格迁移里，现在最像“原策略缺口”的只剩 select_layer 这件事了。LLaVA 明确是
  hidden_states[self.select_layer] 做 feature，而 Qwen 现在根本没有这个概念，所以我先查本地模型配置里这个值。我已经把改动收束到“只补 select_layer=-2 这个 LLaVA 原策略缺口”上了，没有碰阶段配比、路由阈值、也没有再改
  fullatt 融合。现在先做代码核对和编译检查，确认没有把别的路径带坏。

  刚才那步 group feature 改动已经撤回。现在保留的只是一处严格的迁移修复：把 LLaVA 的 select_layer 机制迁到
  Qwen，默认按你本地 LLaVA 配置里的 mm_vision_select_layer=-2 来取选择特征。V105/V139 的阶段划分、auto
  router、fullatt 层使用方式都没改。相关改动在 yaohui/StaPrune_main/Qwen2_5_VL/qwen2_5_vl_StaPrune.py:423 和
  yaohui/StaPrune_main/Qwen2_5_VL/qwen2_5_vl_StaPrune.py:1026。

  具体做法是：Qwen 之前一直拿最后一层输出做 selection feature，这和 LLaVA 的
  hidden_states[self.select_layer] 不一致。现在改成在视觉 block 循环里捕获 -2 层 hidden state 来算选中索引，
  但真正送进 Qwen merger 的仍然是最终层 group，这一段在 yaohui/StaPrune_main/Qwen2_5_VL/
  qwen2_5_vl_StaPrune.py:819 和 yaohui/StaPrune_main/Qwen2_5_VL/qwen2_5_vl_StaPrune.py:1052。这是为了补齐 LLaVA
  原策略里的 select_layer，不是另起一套剪枝策略。

  我只做了编译检查，py_compile 已通过，还没跑新一轮 POPE，所以这版还没有实验结果。你可以直接重跑原来的 11.1%
  和 33.3% 命令，命令不用加新参数。共享实验记录我已经整理到 yaohui/StaPrune_main/EXPERIMENT_HISTORY.md:1，后
  面每次有效结果都可以直接接着记。